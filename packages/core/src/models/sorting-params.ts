import { OrderingMode } from './ordering-mode';
export interface SortingParams {
  sort: OrderingMode;
  // ordering?: 'ASC' | 'DESC';
  // rank?: boolean;
  // popularity?: boolean;
  // latest?: boolean;
  // TODO: avg_rank_votes?: boolean;
}
